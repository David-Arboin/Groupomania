{
    "scripts": {
        "front": "cd frontend && yarn start",
        "sass": "cd frontend && yarn run sass",
        "back": "cd backend && nodemon",
        "go": "concurrent \"yarn run front\" \"yarn run sass\" \"yarn run back\" "
    },
    "devDependencies": {
        "concurrent": "^1.0.0"
    },
    "dependencies": {
        "classnames": "^2.3.1"
    }
}
